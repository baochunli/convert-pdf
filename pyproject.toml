[project]
name = "pdf-convert"
version = "0.1.0"
description = "Command-line helper for converting PDFs with OpenDataLoader PDF."
requires-python = ">=3.13"
dependencies = [
    "docling",
    "docling[vlm]",
]
